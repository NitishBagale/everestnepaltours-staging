# name: Lint PR and Test

# on:
#   pull_request:
#     types:
#       - opened
#       - edited
#       - synchronize

# permissions:
#   contents: read
#   pull-requests: read

# jobs:
#   validate-pr-title:
#     name: Lint PR Title
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Validate Semantic PR Title
#         uses: amannn/action-semantic-pull-request@v5.1.0
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           # Only validate PR title. Do not validate commits.
#           validateSingleCommit: false
#           validateSingleCommitMatchesPrTitle: false

#           # Allow slightly loose conventional titles
#           subjectPattern: ".+"

#   run-unit-tests:
#     name: Run Unit Tests
#     runs-on: ubuntu-latest
#     needs: validate-pr-title

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       - name: Install dependencies
#         run: npm install

#       - name: Run unit tests
#         run: npm test
