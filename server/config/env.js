const dotenv = require("dotenv");
dotenv.config();

const {
  DATABASE_URL,
  DIRECT_DATABASE_URL,
  DB_NAME,
  DB_USERNAME,
  DB_PASSWORD,
  DB_HOST,
  DB_PORT,
  SSL,
  DB_DIALECT,
  JWT_SECRET,
  ADMIN_MAIL,
  SMTP_EMAIL,
  SMTP_PASSWORD,
  SECRET_KEY,
  PORT,
  NODE_ENV,
} = process.env;

module.exports = {
  DATABASE_URL,
  DIRECT_DATABASE_URL,
  DB_NAME,
  DB_USERNAME,
  DB_PASSWORD,
  DB_HOST,
  DB_PORT,
  SSL,
  DB_DIALECT,
  JWT_SECRET,
  ADMIN_MAIL,
  SMTP_EMAIL,
  SMTP_PASSWORD,
  SECRET_KEY,
  PORT,
  NODE_ENV,
};
